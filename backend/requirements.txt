# === Core Framework ===
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
httpx==0.27.0
sse-starlette==2.1.0

# === LangChain / LangGraph ===
langgraph==0.2.14
langchain==0.2.14
langchain-core==0.2.32
langchain-groq==0.1.9
langchain-community==0.2.12

# === Vector Store & Embeddings ===
# Pinned to an exact CPU-only build — without this, torch resolves to
# whatever the latest GPU build is and drags in ~2GB of unused CUDA/cuDNN
# libraries (nvidia-cublas-cu12, nvidia-cudnn-cu12, nvidia-nccl-cu12, triton,
# etc.), confirmed by an actual test resolution pulling 31 nvidia-* packages.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu
chromadb==0.5.5
sentence-transformers==3.0.1

# Pinned exactly to stop pip backtracking through chromadb's loosely
# constrained transitive deps — verified against a real resolution that
# previously took 90s+ of searching; with these pinned it resolves in ~11s.
opentelemetry-api==1.27.0
opentelemetry-sdk==1.27.0
opentelemetry-exporter-otlp-proto-common==1.27.0
opentelemetry-exporter-otlp-proto-grpc==1.27.0
opentelemetry-proto==1.27.0
opentelemetry-instrumentation==0.48b0
opentelemetry-instrumentation-asgi==0.48b0
opentelemetry-instrumentation-fastapi==0.48b0
protobuf==4.25.9
SQLAlchemy==2.0.51
posthog==7.19.2
scipy==1.17.1
scikit-learn==1.9.0
huggingface_hub==0.36.2
tokenizers==0.22.2
langchain-text-splitters==0.2.2
langgraph-checkpoint==1.0.8

# === LLM APIs ===
groq==0.9.0

# === Search ===
tavily-python==0.3.3

# === GitHub ===
PyGithub==2.3.0

# === Parsers ===
pdfplumber==0.11.1
python-docx==1.1.2

# === PDF Export ===
weasyprint==62.3
jinja2==3.1.4

# === Data & Validation ===
pydantic==2.8.2
pydantic-settings==2.4.0
python-dotenv==1.0.1

# === Utilities ===
asyncio-throttle==1.0.2
tenacity==8.5.0
structlog==24.4.0
numpy==1.26.4